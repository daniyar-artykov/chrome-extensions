(function() {
    function g(b) {
        if (b && (b = b[e + d])) {
            var a = document.forms.jpform;
            if (a) {
                var c = !0;
                b.fromStation ? b.toStation ? b.doj || (a["jpform:journeyDateInputDate"].focus(), c = !1) : (a["jpform:toStation"].focus(), c = !1) : (a["jpform:fromStation"].focus(), c = !1);
                a["jpform:fromStation"] && (a["jpform:fromStation"].value = b.fromStation || "");
                a["jpform:toStation"] && (a["jpform:toStation"].value = b.toStation || "");
                a["jpform:journeyDateInputDate"] && (a["jpform:journeyDateInputDate"].value = b.doj || "");
                a["jpform:ticketType"] && (a["jpform:ticketType"].value =
                    b.ticketType || "E_TICKET");
                a["jpform:handicapPassengers"] && (a["jpform:handicapPassengers"].checked = b.phConcession || !1);
                a["jpform:jpsubmit"] && c && a["jpform:jpsubmit"].click()
            }
        }
    }
    var f = !0;
    chrome.storage.local.get("irctcZapTatkalEnable", function(b) {
        b && (f = b.irctcZapTatkalEnable);
        f && chrome.storage.local.get("selectedForm", function(a) {
            a && (d = a.selectedForm, chrome.storage.local.get(e + d, g))
        })
    });
    var d = void 0,
        e = "jpDetails_"
})();